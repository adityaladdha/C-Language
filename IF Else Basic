#include <stdio.h>

int main()
{
    int age;
    printf("The age of AL = ",age);
    scanf("%d",&age);
    if(age>18)
    {
        printf("AL is eligible for voting");
    }
    else
    {
        printf("AL is not eligible for voting");
    }
    return 0;
}
