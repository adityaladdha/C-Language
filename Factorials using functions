#include<stdio.h>

int fact(int);

void main()
{
    int f, n;
    printf("Enter the number :");
    scanf("%d",&n);
    f = fact(n);
    printf("%d",f);
}

int fact(int n)
{
    int i, f=1;
    for(i=1;i<=n;i++)
    {
        f=f*i;
    }
    return f;
}
